server:
  port: 5555

shiro:
  ws:
    client:
      enable: true
      url: "ws://192.168.100.77:3000"
    server:
      enable: false
      url: "/ws/shiro"
logging:
  level:
    com.kennycason.kumo: OFF
  file:
    name: logs/yuri.log

spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    open-in-view: false
    # database-platform: org.hibernate.community.dialect.SQLiteDialect

